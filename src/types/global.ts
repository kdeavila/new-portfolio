declare global {
    interface CardProps {
        image: string;
        title: string;
        desc: string;
        tags: string[];
        github: string;
        demo: string;
        timer: number;
    }
}

export { };
